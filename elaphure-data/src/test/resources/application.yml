spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:elaphure;NON_KEYWORDS=user
    username: root
    password: admin

logging:
  level:
    com.tsengfhy.elaphure.mapper: debug